# All of these variables must set in your .bashrc instead here because they are sensitive.
export ${AWS_SECRET_ACCESS_KEY}
export ${AWS_ACCESS_KEY_ID}
export ${AWS_S3_BUCKET}
export ${ROLLBAR_ACCESS_TOKEN}
